{% extends "FulldonIntersaBundle::coque_intersa.html.twig" %}
    {% block coque_intersa %}
        <div class="breadcrumbs" id="breadcrumbs">
            <ol class="breadcrumb">
                <li><a href="{{ path('fulldon_intersa_homepage') }}">Accueil</a></li>
                <li class="active">Configuration avancée</li>
            </ol>
        </div>
        <div class="page-content">
            <div class="page-header">
                <h1>
                    Configuration avancée
                    <small>
                        <i class="ace-icon fa fa-angle-double-right"></i>
                        Configurer votre fulldon
                    </small>
                </h1>
            </div>
            <!-- /.page-header -->
            {% for flashMessage in app.session.flashbag.get('info') %}
                <div class="alert alert-success">
                    {{ flashMessage }}
                </div>
            {% endfor %}
            {% for flashMessage in app.session.flashbag.get('erreur') %}
                <div class="alert alert-danger">
                    {{ flashMessage }}
                </div>
            {% endfor %}
            {{ form_errors(form) }}
            <form action="{{ path('intersa_conf_avance') }}" method="post" class="form-group" {{ form_enctype(form) }}>

                <div class="row">
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.seuilRf, "Seuil en euros pour la génération des reçus fiscaux : ") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.seuilRf) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.seuilRf, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.seuilPnd, "Seuil des PNDs pour la désactivation du compte donateur") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.seuilPnd) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.seuilPnd, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>

                <div class="row">

                </div>
                <div class="row">
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.assocIban, "Iban de l'association") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.assocIban) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.assocIban, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.assocBic, "Bic de l'association") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.assocBic) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.assocBic, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.seuilRf, "Le code Sepa de l'association") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.assocSepa) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.assocSepa, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.assocBanqueName, "assocBanqueName") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.assocBanqueName) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.assocBanqueName, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.jourPrelevement, "Le jour de prélèvement") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.jourPrelevement) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.jourPrelevement, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.googleAnalytics, "Google analytics") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.googleAnalytics) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.googleAnalytics, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.twitterId, "Compte twitter") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.twitterId) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.twitterId, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputEmail3"
                               class=" control-label">{{ form_label(form.facebookId, "Compte facebook") }}</label>
                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.facebookId) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.facebookId, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-group col-md-6">
                        {{ form_widget(form.generateRf) }}{{ form_label(form.generateRf, "Générez automatiquement les reçus fiscaux des dons validés") }} <br />
                        {{ form_widget(form.sendStats) }}{{ form_label(form.sendStats, "Envoyez les statistiques journalières par email") }}
                    </div>
                    <div class="form-group col-md-6">
                        <label class=" control-label">{{ form_label(form.maxPerPage, "Maximum de résultats par page") }}</label>

                        <div>
                            {# Affichage des erreurs pour ce champ précis. #}
                            {{ form_errors(form.maxPerPage) }}

                            {# Génération de l'input. #}
                            {{ form_widget(form.maxPerPage, {'attr': {'class': 'task_field form-control'} }) }}
                        </div>
                    </div>
                </div>
                <div class="row">

                </div>
                <div class="row">
                    <div class="widget-toolbox padding-8 clearfix">

                        <button class="btn btn-xs btn-success pull-right" type="submit">
                            <span class="bigger-110">Valider la configuration</span>

                            <i class="ace-icon fa fa-arrow-right icon-on-right"></i>
                        </button>
                    </div>
                </div>
                {{ form_rest(form) }}
            </form>
        </div>

    {% endblock %}
    {% block javascripts %}
        {{ parent() }}
    {% endblock %}